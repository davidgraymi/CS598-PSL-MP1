[project]
name = "cs598-psl-mp1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Gray", email = "davidgraymi@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
    "ucimlrepo>=0.0.7",
]

[project.scripts]
cs598-psl-mp1 = "cs598_psl_mp1:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "nbconvert[webpdf]>=7.17.1",
]
